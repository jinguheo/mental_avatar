# 3D 아바타(GLB) 만들기 가이드

mental-avatar의 "실사 아바타" 탭(`RealisticAvatar.tsx`, http://localhost:5174)에서 쓰는
3D 아바타 모델(.glb)을 준비하고 등록하는 방법입니다.

## 1. 사진 준비 (직접 촬영 필요)

- 본인 정면 얼굴 사진 1장
- 밝은 곳에서, 얼굴 전체가 가려지지 않게

## 2. Avaturn에서 GLB 생성

1. https://avaturn.me 접속 → 회원가입/로그인
2. 사진 업로드 → 자동으로 3D 아바타 생성
3. 헤어스타일/의상 등 원하는 대로 커스터마이즈
4. 완성 후 **Download → glTF Binary (.glb)** 선택해서 다운로드

> 예전엔 Ready Player Me를 썼으나 2026-01-31 서비스 종료로 Avaturn으로 전환했습니다.
> Avaturn 무료 다운로드는 ARKit blendshape가 포함되지 않아, 입모양은 실제 발음이 아니라
> TTS 오디오 주파수 기반 근사치로 움직입니다(완전히 정확하진 않음).

## 3. mental-avatar에 등록

1. mental-avatar(5174) → **실사 아바타** 탭 이동
2. 좌상단 **"📁 GLB 파일 선택"** 클릭 → 다운로드한 .glb 선택
   (또는 화면에 파일을 그대로 드래그&드롭)
3. 로드되면 브라우저에 자동 저장(IndexedDB) — 다음 접속 시 마지막 아바타가 자동으로 뜸

## 4. 여러 개 관리

- 여러 GLB를 등록해두면 좌상단 **"▼ N개"** 버튼으로 목록이 펼쳐짐
- 목록에서 클릭하면 해당 아바타로 전환, ✕ 클릭하면 삭제

## 참고

- GLB는 서버가 아니라 **브라우저(IndexedDB)**에만 저장됩니다. PC를 옮기거나 브라우저
  데이터를 지우면 다시 만들어서 등록해야 합니다(서버 쪽 백업/복원 대상이 아님).
- 립싱크/얼굴교체(SadTalker) 기능에 쓰는 **정지 얼굴 사진**(`data/face.jpg`)은 이 GLB와
  별개입니다 — GLB는 3D 실사 아바타 화면 전용입니다.
